<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Molecula</title>
  <link rel="stylesheet" href="/styles.css" />
  <script>
    (function applyUiState() {
      const theme = localStorage.getItem("theme") || "light";
      const lang = localStorage.getItem("lang") || "ru";
      document.documentElement.setAttribute("data-theme", theme);
      document.documentElement.setAttribute("lang", lang);
    })();
  </script>
</head>
<body>
  <header class="board-header">
    <div class="board-header-top">
      <a href="/">Главная</a>
      <a href="/requisites">Реквизиты</a>
      <a href="/">Каталог</a>
      <span class="header-right-state">Molecula online</span>
    </div>
    <div class="board-header-main">
      <a class="board-logo" href="/">Molecula</a>
      <div class="header-center-nav">
        <a href="/">Лента</a>
        <a href="/polls/new" class="thread-create-link">Создать опрос</a>
      </div>
      <div class="header-controls">
        <button type="button" id="theme-toggle" class="tiny-btn">Тема</button>
        <button type="button" id="lang-toggle" class="tiny-btn">Язык</button>
      </div>
    </div>
  </header>

  <div class="quick-dock">
    <a class="dock-item" href="/requisites" title="Реквизиты">₽</a>
    <% if (currentUser && currentUser.is_admin) { %>
      <a class="dock-item" href="/admin" title="Админ">A</a>
    <% } %>
  </div>

  <main class="page-wrap">
    <section class="user-toolbar">
      <% if (currentUser) { %>
        <a href="/me" class="toolbar-btn">Кабинет</a>
        <% if (currentUser.is_admin) { %>
          <a href="/admin" class="toolbar-btn">Админ</a>
        <% } %>
        <form action="/logout" method="POST">
          <button type="submit" class="toolbar-btn toolbar-btn-primary">Выйти</button>
        </form>
      <% } else { %>
        <a href="/login" class="toolbar-btn">Вход</a>
        <a href="/register" class="toolbar-btn toolbar-btn-primary">Регистрация</a>
      <% } %>
    </section>

    <% if (flash) { %>
      <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <% } %>
